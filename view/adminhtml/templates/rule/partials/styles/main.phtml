<style>
/* Loading Overlay */
.builder-loading-overlay {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: rgba(0,0,0,0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10000;
}
.loading-spinner {
    background: #fff;
    padding: 25px 40px;
    border-radius: 4px;
    text-align: center;
    box-shadow: 0 4px 20px rgba(0,0,0,0.2);
}
.spinner {
    border: 3px solid #e8e8e8;
    border-top: 3px solid #eb5202;
    border-radius: 50%;
    width: 36px; height: 36px;
    animation: spin 0.8s linear infinite;
    margin: 0 auto 10px;
}
@keyframes spin { to { transform: rotate(360deg); } }
#loading-message { margin: 0; color: #303030; font-size: 14px; }

/* Notification System */
.builder-notifications {
    position: fixed;
    top: 15px; right: 15px;
    z-index: 10001;
    display: flex;
    flex-direction: column;
    gap: 8px;
}
.notification-item {
    padding: 12px 16px;
    border-radius: 4px;
    font-size: 13px;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 8px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    min-width: 250px;
}
.notification-success { background: #e6f9ed; color: #185b00; border: 1px solid #86efac; }
.notification-error { background: #fef2f2; color: #991b1b; border: 1px solid #fca5a5; }
.notification-info { background: #eff6ff; color: #1e40af; border: 1px solid #93c5fd; }

/* FontAwesome grid in modal */
.fa-icon-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(40px, 1fr));
    gap: 4px;
    max-height: 400px;
    overflow-y: auto;
}
.fa-icon-option {
    width: 40px; height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid transparent;
    border-radius: 3px;
    cursor: pointer;
    font-size: 16px;
    color: #555;
}
.fa-icon-option:hover { border-color: #006bb4; background: #f5f9fc; color: #006bb4; }
.fa-icon-option.selected { border-color: #eb5202; background: #fff7f2; color: #eb5202; }

/* Category tree in chooser */
.category-tree-item { padding: 4px 0; }
.category-tree-item label { display: flex; align-items: center; gap: 6px; cursor: pointer; font-size: 13px; }
.category-tree-children { padding-left: 20px; }

/* Product list in chooser */
.product-chooser-item {
    display: flex; align-items: center; gap: 8px;
    padding: 6px 0; border-bottom: 1px solid #f0f0f0;
    font-size: 13px;
}
.product-chooser-item:last-child { border-bottom: none; }

/* Preview badge rendering */
.preview-badge {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 12px;
    font-weight: 600;
    color: #fff;
    position: absolute;
    z-index: 10;
}
</style>
